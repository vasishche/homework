#!/bin/bash
# Task No 2

find /usr/share/man -name "*help*"

find /usr/share/man -name "conf*"

# Мы можем протестировать на принадлежность к группе, наличие прав, последнее время чтения/записи/выполнения файла, сравнить с другим файлом. Кроме вывода в stdout, можем вывести в файл с нужным форматированием, можем удалить или выполнить команду над файлами. Например:
# выводим файлы(каталоги) с правами на выполнение(поиск) у other в формате ls:

find ~/devOps -perm /o+x -ls

# отнимаем права на выполнение(поиск) у other для тех файлов/каталогов у которых они есть:

find ~/devOps -perm /o+x -exec chmod o-x '{}' \;
